var reverseString = function (s) {
    // The input 's' is an array of characters.
    // The built-in Array method 'reverse()' reverses the array *in place*,
    // meaning it modifies the original array rather than returning a new one.
    s.reverse();
};
